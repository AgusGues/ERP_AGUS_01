@using System.Data
@model DataTable

@{
    ViewData["Title"] = "Detail Purchase Order";
}

<div class="text-end">
    <a href="/PurchaseOrder/PODetailAll" class="btn btn-info btn-sm">View All Purchase Order Detail</a>
</div>

<h4>Detail Purchase Order</h4>

<table class="table table-bordered table-striped">
    <thead class="table-dark">
        <tr>
                <th>PO Id</th>
                <th>PO Number</th>
                <th>PO Date</th>
                <th>Supplier Name</th>
                <th>Item Name</th>
                <th class="text-end">Qty</th>
                <th class="text-end">Price</th>
        </tr>
    </thead>

    <tbody>
        
        @foreach (DataRow r in Model.Rows)
        {
            <tr>
                <td>@r["POId"]</td>
                <td>@r["PONumber"]</td>
                <td>@Convert.ToDateTime(@r["PODate"]).ToString("dd/MM/yyyy")</td>
                <td>@r["SupplierName"]</td>
                <td>@r["ItemName"]</td>
                <td class="text-end">@r["Qty"]</td>
                <td class="text-end">@r["Price"]</td>
            </tr>
            
        }
    </tbody>
    
</table>

<a href="/PurchaseOrder/PODetailAll" class="btn btn-secondary">
    Kembali
</a>
